<?xml version="1.0" encoding="UTF-8"?>
<chapter xmlns="http://docbook.org/ns/docbook" id="chapter-actions-and-submission" xmlns:xi="http://www.w3.org/2001/XInclude">
    <title>Actions and Submission (XForms 1.1 Chapters 10 and 11)</title>
    <para>This chapter documents action dispatch semantics, context resolution, and submission processing behavior including request serialization, headers, and response handling.</para>

    <section id="chapter-actions-mapping">
        <title>Actions: XForms to implementation hooks</title>

        <section id="chapter-actions-dispatch-pipeline">
            <title>Action dispatch pipeline</title>
            <para>When a DOM event fires (e.g., button click), the corresponding <literal>ixsl:onclick</literal> mode template (<literal>L3727</literal>) routes to <literal>applyActions</literal> (<literal>L4666</literal>). This template retrieves the action map for the control and invokes <literal>outermost-action-handler</literal> (<literal>L6589</literal>), which processes the outermost action container. For nested actions, <literal>xforms-event-handler</literal> (<literal>L6655</literal>) handles recursive dispatch.</para>
            <para>Event-action matching uses <literal>get-matching-event-actions()</literal> (<literal>L1283</literal>), which filters action maps by event name and observer ID. Event default cancellation is checked via <literal>is-event-default-cancelled()</literal> (<literal>L1304</literal>).</para>
        </section>

        <section id="chapter-actions-map-builder">
            <title>Action map builder</title>
            <para>During control rendering, the <literal>set-action</literal> mode template (<literal>L847</literal>) builds an action map (<literal>map(*)</literal>) for each action element. The map stores:</para>
            <itemizedlist>
                <listitem><para><literal>name</literal> — The action type (setvalue, insert, delete, toggle, dispatch, send, etc.).</para></listitem>
                <listitem><para><literal>ref</literal> — The effective nodeset reference, resolved via <literal>get-properties</literal> mode.</para></listitem>
                <listitem><para><literal>instanceContext</literal> — The instance ID the action operates on.</para></listitem>
                <listitem><para><literal>if</literal>/<literal>while</literal>/<literal>iterate</literal> — Guard conditions from <literal>@if</literal>, <literal>@while</literal>, <literal>@iterate</literal> attributes.</para></listitem>
                <listitem><para><literal>bindRef</literal> — Optional <literal>@bind</literal> reference for bind-based action resolution.</para></listitem>
            </itemizedlist>
            <para>Action-map persistence now keeps bind metadata, and insert/delete resolve local context first with absolute-ref fallback (resolved: 10.4.b, 10.18.b).</para>
        </section>

        <section id="chapter-actions-individual">
            <title>Individual action templates</title>
            <variablelist>
                <varlistentry><term>action-setvalue (L6782)</term><listitem><para>Evaluates the <literal>@value</literal> expression or uses element text content to set the bound instance node. Delegates to <literal>action-setvalue-inner</literal> (<literal>L6854</literal>) for XPath evaluation and to <literal>action-setvalue-form-control</literal> (<literal>L7018</literal>) for DOM control update.</para></listitem></varlistentry>
                <varlistentry><term>action-insert (L7152)</term><listitem><para>Clones and inserts nodes into the instance. Supports <literal>@origin</literal>, <literal>@at</literal>, <literal>@position</literal> (before/after), and <literal>@context</literal> attributes. Uses <literal>insert-node</literal> mode (<literal>L3622</literal>) for DOM-level insertion. Known issues: multi-instance <literal>@context</literal> resolution (10.3.a/c), <literal>@bind/@model</literal> crash (10.3.b), <literal>@at</literal> position evaluation (10.3.d, 10.4.d).</para></listitem></varlistentry>
                <varlistentry><term>action-delete (L7388)</term><listitem><para>Removes nodes from instances. Uses <literal>delete-node</literal> mode (<literal>L3664</literal>). Known issues: post-delete repeat index tracking (10.4.e/f).</para></listitem></varlistentry>
                <varlistentry><term>action-toggle (L8005)</term><listitem><para>Toggles visibility of <literal>xforms:case</literal> elements within a <literal>xforms:switch</literal>. The <literal>$source-control</literal> scoping mechanism exists (<literal>L6018–6024</literal>) but is not always passed through the event dispatch chain. Known issues: toggle inside repeat affects all iterations (9.3.1.f), case child <literal>@value</literal> precedence (10.6.1.b).</para></listitem></varlistentry>
                <varlistentry><term>action-dispatch (L7530)</term><listitem><para>Dispatches a named event to a target, supporting <literal>@targetid</literal>, <literal>@bubbles</literal>, <literal>@cancelable</literal>, and <literal>@delay</literal>. Known issue: <literal>cancelable="true"</literal> + <literal>ev:preventDefault</literal> does not prevent default action (10.8.f).</para></listitem></varlistentry>
                <varlistentry><term>action-message (L7631)</term><listitem><para>Displays a modal message via <literal>alert()</literal>. Known issue: model-level event messages (<literal>xforms-rebuild</literal>, <literal>xforms-reset</literal>, etc.) do not invoke the modal (10.13.a, 10.8.1.a/b, 8.1.8.a, 8.2.2.a–c, 8.2.3.a–c).</para></listitem></varlistentry>
                <varlistentry><term>action-send (L7825)</term><listitem><para>Triggers submission by looking up the submission map and invoking the submission flow.</para></listitem></varlistentry>
                <varlistentry><term>action-setindex (L7845)</term><listitem><para>Sets the repeat index for a given repeat ID.</para></listitem></varlistentry>
                <varlistentry><term>action-setfocus (L7804)</term><listitem><para>Sets focus to a control. Enhanced with suffixed-ID fallback and group→first-child focus (resolved: 9.1.1.c, 10.7.a).</para></listitem></varlistentry>
                <varlistentry><term>action-reset (L7948)</term><listitem><para>Restores instance data from initial snapshots saved during model construction (resolved: 10.a).</para></listitem></varlistentry>
                <varlistentry><term>action-recalculate (L7913) / action-revalidate / action-refresh (L7927)</term><listitem><para>Explicitly trigger the corresponding lifecycle phase. <literal>action-revalidate</literal> invokes <literal>xforms-revalidate</literal> (including validation MIP registry rebuild used by refresh class projection) and clears the deferred revalidate flag.</para></listitem></varlistentry>
                <varlistentry><term>action-script (L7741)</term><listitem><para>Fork enhancement: XForms 2.0 script execution via <literal>ixsl:call(window, 'eval', ...)</literal>.</para></listitem></varlistentry>
                <varlistentry><term>action-load (L7764)</term><listitem><para>Fork enhancement: Supports <literal>show="new"</literal> (window.open), <literal>show="replace"</literal> (location.href), and <literal>javascript:</literal> URI scheme.</para></listitem></varlistentry>
            </variablelist>
        </section>
    </section>

    <section id="chapter-actions-dispatch-diagram">
        <title>Action dispatch sequence diagram</title>
        <para>The following sequence shows the event-to-action resolution pipeline and execution flow for each action type.</para>
        <mediaobject>
            <imageobject>
                <imagedata fileref="../diagrams/sequence-action-dispatch.svg" format="SVG" width="100%" scalefit="1"/>
            </imageobject>
        </mediaobject>
    </section>

    <section id="chapter-submission-section">
        <title>Submission</title>

        <section id="chapter-submission-overview">
            <title>Submission flow overview</title>
            <para>Submission is initiated by <literal>action-send</literal> (<literal>L7825</literal>), which looks up the submission configuration from the <literal>$submissions</literal> map built during <literal>xformsjs-main</literal> (<literal>L276</literal>). The <literal>xforms-submit</literal> template (<literal>L5729</literal>) orchestrates the full submission lifecycle:</para>
            <orderedlist>
                <listitem><para><emphasis>Pre-submit validation</emphasis> — Runs <literal>xforms-revalidate</literal> (<literal>L5531</literal>) to check required fields and constraints. If validation fails, <literal>dispatch-submit-error</literal> (<literal>L5658</literal>) fires.</para></listitem>
                <listitem><para><emphasis>Serialization</emphasis> — Instance data is serialized based on <literal>@serialization</literal> (default: <literal>application/xml</literal>). The <literal>setSubmission</literal> template (<literal>L4966</literal>) configures the submission parameters.</para></listitem>
                <listitem><para><emphasis>Transport</emphasis> — <literal>xforms-submit</literal> builds an HTTP request map and schedules transport via <literal>ixsl:schedule-action</literal>. The response callback is handled by <literal>HTTPsubmit</literal> (<literal>L1801</literal>), which normalizes status/body/headers before dispatching submit outcome events.</para></listitem>
                <listitem><para><emphasis>Response handling</emphasis> — Response headers are parsed by <literal>response-headers-to-nodes()</literal> (<literal>L1133</literal>). Based on <literal>@replace</literal>: <literal>"instance"</literal> replaces instance data, <literal>"all"</literal> replaces page content, <literal>"none"</literal> dispatches <literal>xforms-submit-done</literal> (<literal>L6545</literal>).</para></listitem>
                <listitem><para><emphasis>Post-submit update</emphasis> — After instance replacement, <literal>xforms-rebuild</literal>/<literal>xforms-recalculate</literal>/<literal>xforms-refresh</literal> are triggered.</para></listitem>
            </orderedlist>
        </section>

        <section id="chapter-submission-known-issues">
            <title>Known submission issues</title>
            <itemizedlist>
                <listitem><para><emphasis>11.1.b</emphasis> — <literal>submission/@bind</literal> is parsed but does not filter submitted instance data to the bound node; the entire instance is sent.</para></listitem>
                <listitem><para><emphasis>11.3.b</emphasis> — <literal>xforms-submit-serialize</literal> event's <literal>submission-body</literal> property is not honored; original instance data is submitted.</para></listitem>
                <listitem><para><emphasis>11.8.1.a, 11.8.2.a</emphasis> — Custom HTTP headers defined via <literal>xforms:header/xforms:name/xforms:value</literal> child elements are not added to the outgoing request.</para></listitem>
            </itemizedlist>
        </section>
    </section>

    <section id="chapter-submission-sequence-diagram">
        <title>Submission sequence diagram</title>
        <para>The sequence captures request build, transport, response parse, and model refresh behavior, including the validation-failure alternative path.</para>
        <mediaobject>
            <imageobject>
                <imagedata fileref="../diagrams/sequence-submission.svg" format="SVG" width="100%" scalefit="1"/>
            </imageobject>
        </mediaobject>
    </section>

    <section id="chapter-actions-and-submission-checkpoint">
        <title>Checkpoint validation focus</title>
        <itemizedlist>
            <listitem><para>Targeted W3C coverage: <literal>tests/xforms/w3c/ch10.spec.ts</literal> (15 tests, 73% pass) and <literal>tests/xforms/w3c/ch11.spec.ts</literal> (submission tests).</para></listitem>
            <listitem><para>Sequence trace verification must cover both <literal>sequence-action-dispatch</literal> and <literal>sequence-submission</literal>, with reproducible <literal>[SEQTRACE]</literal> evidence for event-to-action resolution, payload serialization, and callback points.</para></listitem>
            <listitem><para>Verify insert/delete known issues match STATUS.md gap cluster description.</para></listitem>
            <listitem><para>Run full regression (<literal>npm run test:gate</literal>) before sign-off.</para></listitem>
        </itemizedlist>
    </section>
</chapter>
