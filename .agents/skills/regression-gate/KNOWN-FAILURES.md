# Known Baseline Failures (Human-readable mirror)

This file is informational only.

The authoritative baseline data is stored in:
- `.agents/skills/regression-gate/baselines/*.json`

Current mirror for `issues-spec`:
1. `Issue #21 — Prepend Element Copy (B.1) › clicking insert adds a person from prototype`
2. `Issue #22 — Set Attribute (B.4) › clicking insert copies @rating to item[2]`

Any additional failing test (outside the active suite baseline JSON `allowed_failures`) is a regression.
